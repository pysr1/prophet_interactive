# Import needed function from setuptools
from setuptools import setup

# Create proper setup to be used by pip
setup(name='prophet_interactive',
      version='0.0.1',
      description='a package to visualize prophet models',
      author='Bryant',
      packages=['prophet_interactive'])